.DS_Store
.vercel/

# Python bytecode
__pycache__/
*.pyc

# Generated OG images (uploaded to Cloudflare Images, not served from the repo)
og-export/

# local Notion integration token (never commit)
tools/blog-renderer/.notion-token

# wrangler local state
.wrangler/

# scratch — device_bash cannot delete, so removals land here for JP to bin
_to_delete/

# .impeccable tooling cache
.impeccable/
